Remaining tasks:

> Bound Boxes LED style does not save/restore changed colors
  fgnd/bgnd colors are stored globally in parent app,
  to support storage in config file.
  However, color changes are handled by functions in the bclk_elements class.
  Communicating changes in these colors, between the two layers,
  is not working well (or at all) at this point.
  
> char **color_menu_str could be converted to <string> ;
  that would eliminate one of my pointer obstacles

> convert all errstr references to syslog()
